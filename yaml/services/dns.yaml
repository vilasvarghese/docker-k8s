apiVersion: v1
kind: Pod
metadata:
  name: busybox
spec:
  containers:
    - image: busybox
      name: busybox
      command:
        - sleep
        - "6000"
      imagePullPolicy: IfNotPresent
#  restartPolicy: Always